Ergo Rosen Bridge Watcher Setup: This document provides instructions for setting up a Rosen Bridge watcher, which serves as a cross-chain oracle for the Ergo network. It covers cloning the repository, configuring the environment, setting up Docker for ARM devices (if applicable), pulling Docker images, starting the watcher, and configuring the `local.yaml` file for the Ergo network.